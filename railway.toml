[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run build"

[deploy]
# We use npx serve to handle SPA routing (redirects to index.html)
startCommand = "npx serve -s dist -l $PORT"
restartPolicyType = "ON_FAILURE"
